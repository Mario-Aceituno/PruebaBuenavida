if (filtros != null)
{
    if (!string.IsNullOrWhiteSpace(filtros.CentroDeCosto))
        request.CentroDeCosto = filtros.CentroDeCosto.ToString();

    if (!string.IsNullOrWhiteSpace(filtros.TextoBusqueda))
    {
        var texto = filtros.TextoBusqueda.Trim();

        if (texto.Contains("--"))
            request.DniIdentidad = texto.Replace("-", "");
        else
            request.NumeroDabuenavida = texto;
    }
} bool Cargando { get; set; }
    [Parameter] public int PaginaActual { get; set; }
    [Parameter] public EventCallback OnPaginaAnterior { get; set; }
    [Parameter] public EventCallback OnPaginaSiguiente { get; set; }
}
