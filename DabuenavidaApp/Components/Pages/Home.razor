builder.Services.AddScoped(sp =>
{
    var config = sp.GetRequiredService<IConfiguration>();
    var apiBaseUrl = config["LotesConfig:apiBaseUrl"] ?? "http://localhost:5271";

    return new HttpClient
    {
        BaseAddress = new Uri(apiBaseUrl)
    };
});