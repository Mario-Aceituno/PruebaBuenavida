// Al construir la URL pública devuelta en FileUploadResult:
var relativePath = relativeFilePath.Replace("\\", "/");

// Si la ruta contiene wwwroot/, la limpiamos para el navegador
if (relativePath.StartsWith("wwwroot/"))
{
    relativePath = relativePath.Substring("wwwroot".Length);
}

var publicUrl = $"{_options.CdnBaseUrl.TrimEnd('/')}/{relativePath.TrimStart('/')}";
